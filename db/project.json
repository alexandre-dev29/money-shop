{
  "name": "db",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "db/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["db/**/*.ts"]
      }
    }
  },
  "tags": []
}
