{
  "name": "config",
  "$schema": "../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "config/src",
  "projectType": "library",
  "targets": {
    "lint": {
      "executor": "@nx/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["config/**/*.ts"]
      }
    }
  },
  "tags": []
}
